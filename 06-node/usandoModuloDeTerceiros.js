// npm i loadsh para instalar a lib
// a lib lodash é referenciado pelo underscore _
const _ = require('lodash')

// metodo do lodash gera numero aleatório
setInterval(() => console.log(_.random(1, 1000)))